name: Process Watering Records

on:
  issues:
    types: [opened, edited, labeled]
  workflow_dispatch:
    inputs:
      force:
        description: 'Force update records'
        required: false
        default: 'false'

jobs:
  process-records:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: read
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'
      
      - name: Create data directory
        run: mkdir -p data
      
      - name: Create records processor
        run: |
          cat > process-records.js << 'EOF'
          const fs = require('fs');
          const https = require('https');
          
          // 配置
          const repo = process.env.GITHUB_REPOSITORY;
          const token = process.env.GITHUB_TOKEN;
          const outputFile = 'data/records.json';
          const requiredLabel = 'watering-record';
          
          console.log('Starting records processing...');
          console.log(`Repository: ${repo}`);
          
          // 简单的API请求函数
          function makeRequest(options, postData = null) {
            return new Promise((resolve, reject) => {
              const req = https.request(options, (res) => {
                let data = '';
                
                res.on('data', (chunk) => {
                  data += chunk;
                });
                
                res.on('end', () => {
                  if (res.statusCode >= 200 && res.statusCode < 300) {
                    try {
                      resolve(JSON.parse(data));
                    } catch (e) {
                      resolve(data);
                    }
                  } else {
                    console.error(`HTTP Error: ${res.statusCode}`);
                    console.error(`Response: ${data}`);
                    reject(new Error(`HTTP Error: ${res.statusCode}`));
                  }
                });
              });
              
              req.on('error', (e) => {
                console.error(`Request error: ${e.message}`);
                reject(e);
              });
              
              if (postData) {
                req.write(postData);
              }
              
              req.end();
            });
          }
          
          // 获取所有带标签的Issues（包括分页）
          async function getAllWateringRecordIssues() {
            console.log('Fetching all watering record issues...');
            let allIssues = [];
            let page = 1;
            let hasMore = true;
            
            while (hasMore) {
              const options = {
                hostname: 'api.github.com',
                path: `/repos/${repo}/issues?state=all&labels=${requiredLabel}&page=${page}&per_page=100`,
                method: 'GET',
                headers: {
                  'User-Agent': 'GitHub-Action-Script',
                  'Accept': 'application/vnd.github.v3+json',
                  'Authorization': `token ${token}`
                }
              };
              
              try {
                const issues = await makeRequest(options);
                if (issues.length === 0) {
                  hasMore = false;
                } else {
                  allIssues = allIssues.concat(issues);
                  page++;
                }
              } catch (error) {
                console.error('Error fetching issues:', error);
                hasMore = false;
              }
            }
            
            console.log(`Found ${allIssues.length} watering record issues`);
            return allIssues;
          }
          
          // 从Issue内容中提取记录数据
          function extractRecordFromIssue(issue) {
            const record = {
              id: issue.number.toString(),
              issue_number: issue.number,
              issue_title: issue.title,
              issue_url: issue.html_url,
              created_at: issue.created_at,
              updated_at: issue.updated_at,
              state: issue.state
            };
            
            // 尝试从Issue内容中提取数据
            if (issue.body) {
              const typeMatch = issue.body.match(/\*\*花卉种类\*\*:\s*(.*?)(?:\r?\n|$)/);
              if (typeMatch) record.type = typeMatch[1].trim();
              
              const dateMatch = issue.body.match(/\*\*浇水日期\*\*:\s*(.*?)(?:\r?\n|$)/);
              if (dateMatch) record.date = dateMatch[1].trim();
              
              const userMatch = issue.body.match(/\*\*记录人\*\*:\s*(.*?)(?:\r?\n|$)/);
              if (userMatch) record.user = userMatch[1].trim();
              
              const timestampMatch = issue.body.match(/\*\*时间戳\*\*:\s*(.*?)(?:\r?\n|$)/);
              if (timestampMatch) record.timestamp = timestampMatch[1].trim();
              
              const idMatch = issue.body.match(/\*\*记录ID\*\*:\s*(.*?)(?:\r?\n|$)/);
              if (idMatch) record.id = idMatch[1].trim();
            }
            
            // 从标题中提取信息（如果内容中没有）
            if (!record.type && issue.title.includes(':')) {
              const titleParts = issue.title.split(':');
              if (titleParts.length > 1 && titleParts[1].includes('-')) {
                const typeParts = titleParts[1].split('-');
                if (typeParts.length > 0) record.type = typeParts[0].trim();
                if (typeParts.length > 1) record.user = typeParts[1].trim();
              }
            }
            
            return record;
          }
          
          // 主函数
          async function main() {
            try {
              // 获取所有带标签的Issues
              const issues = await getAllWateringRecordIssues();
              
              if (issues.length === 0) {
                console.log('No watering record issues found. Creating empty records file.');
                fs.writeFileSync(outputFile, '[]');
                return;
              }
              
              // 处理每个Issue
              const records = issues.map(issue => {
                console.log(`Processing issue #${issue.number}: ${issue.title}`);
                return extractRecordFromIssue(issue);
              }).filter(record => record.type); // 只保留有花卉类型的记录
              
              // 按时间排序（最新的在前）
              records.sort((a, b) => {
                const dateA = a.timestamp ? new Date(a.timestamp) : new Date(a.created_at);
                const dateB = b.timestamp ? new Date(b.timestamp) : new Date(b.created_at);
                return dateB - dateA;
              });
              
              // 写入文件
              console.log(`Writing ${records.length} records to ${outputFile}`);
              fs.writeFileSync(outputFile, JSON.stringify(records, null, 2));
              
              // 提交更改
              console.log('Committing changes...');
              const commitMessage = `Update records from issues (${new Date().toISOString()})`;
              await exec('git config --global user.name "GitHub Actions"');
              await exec('git config --global user.email "actions@github.com"');
              await exec('git add data/records.json');
              await exec(`git commit -m "${commitMessage}"`);
              await exec('git push');
              
              console.log('Records processing completed successfully');
            } catch (error) {
              console.error('Error processing records:', error);
              // 创建空记录文件，避免页面加载错误
              fs.writeFileSync(outputFile, '[]');
              // 不抛出错误，让工作流继续
            }
          }
          
          // 执行主函数
          main();
          
          // 辅助函数：执行shell命令
          function exec(command) {
            return new Promise((resolve, reject) => {
              require('child_process').exec(command, (error, stdout, stderr) => {
                if (error) {
                  console.error(`exec error: ${error}`);
                  reject(error);
                } else {
                  console.log(stdout);
                  resolve(stdout);
                }
              });
            });
          }
          EOF
          
          echo "Running records processor..."
          node process-records.js
      
      - name: Commit and push changes
        if: always()
        run: |
          git config --global user.name 'GitHub Actions'
          git config --global user.email 'actions@github.com'
          git add data/records.json
          git diff --staged --quiet || (git commit -m "Update records from issues [skip ci]" && git push)
